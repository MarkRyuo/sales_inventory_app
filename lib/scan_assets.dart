import 'package:flutter/material.dart';

class ScanAssetTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Scan Asset Tab'),
    );
  }
}
